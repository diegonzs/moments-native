import { ScrollView } from 'react-native'

import { Box } from '../box/'

export const List = () => {
  return (
    <ScrollView horizontal className="gap-x-2">
      <Box />
      <Box />
      <Box />
      <Box />
      <Box />
    </ScrollView>
  )
}
